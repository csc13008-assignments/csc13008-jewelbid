export enum UserRole {
    BIDDER = 'bidder',
    SELLER = 'seller',
    ADMIN = 'admin',
}

export enum AuthType {
    OAUTH = 'oauth',
    STD_AUTH = 'std_auth',
}

export enum AuthProvider {
    GOOGLE = 'google',
    LOCAL = 'local',
}
